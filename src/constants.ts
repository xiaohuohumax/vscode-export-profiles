export const app = 'vscode-export-profiles';

export const cKeys = {
  exportProfiles: app + '.exportProfiles'
};

export const defaultCodeProfileName = 'index.code-profile';

export default {
  app,
  cKeys,
  defaultCodeProfileName
};